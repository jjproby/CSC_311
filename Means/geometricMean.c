#include <math.h>

/**
* Creates the geometric mean of the two arguments.
* Multiplys the two arguments and then returns the square root
**/

double geometricMean( double x, double y) {
  return sqrt(x*y);
}
