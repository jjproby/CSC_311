#include <math.h>

/**
* Really, what do you think this does?
* It just divides something you idiot.
* It takes the sum of the generated numbers
* and divides by the amount of numbers generated. Idiot.
* \param sum The sum of all the generated numbers.
* \param len The amount of all generated numbers.
**/
float average( int sum, int len){
  return (float)sum/len;
}
